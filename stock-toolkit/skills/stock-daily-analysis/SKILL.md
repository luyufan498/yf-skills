---
name: stock-daily-analysis
description: 执行系统化的股票深度分析，通过多维度信息收集、自适应搜索链路和专业数据整合，生成全面、深度的股票分析报告。包含三层分析框架（公司业务、行业趋势、短期线索）、历史分析回顾、广发证券专业数据整合等功能。当需要分析单只股票时使用：获取实时股价、深度搜索新闻、整合龙虎榜/财务基本面/ETF数据、生成投资建议并保存分析报告。
---

# 股票深度分析

执行系统化的股票深度分析流程，通过自适应搜索和多数据源整合，生成专业的股票分析报告。

## 核心流程

分析流程采用并行执行策略，通过多个专用 subagent 同时处理不同维度任务：

- [ ] 获取股价数据 - 实时股价、涨跌幅、K 线趋势 + 标准化股票信息
- [ ] 读取历史分析 - 建立分析连续性和趋势对比
- [ ] 自适应深度搜索 - 多轮迭代收集新闻和热点
- [ ] 获取专业数据 - 龙虎榜、财务基本面、ETF 榜单
- [ ] 整合中间数据 - 读取所有 subagent 保存的中间数据
- [ ] 获取模拟盘持仓 - 读取当前持仓状态和资金池信息
- [ ] 生成并保存分析报告 - 三层分析框架 + 模拟盘状态 + 结构化存储
- [ ] 输出执行摘要 - 完成状态和关键信息总结

**执行特点**：
- ⚡ **并行执行**：历史、搜索、广发数据三个 subagent 并行工作
- 🔗 **统一管理**：所有 subagent 由主 session 创建，确保完全隔离
- 📊 **多维整合**：历史、新闻、专业数据、股价全面融合


## 执行步骤

### 步骤 1: 获取股价数据

**主 agent 直接执行**：使用 ptrade CLI 工具获取实时股价、K 线数据以及标准化的股票名称和代码。

**执行原则**（主 agent 操作）：
- 主 agent 直接使用 ptrade 命令
- 用户输入的名称可能不准确，进行模糊匹配和验证，确保获取到正确的股票名称和代码，供后续步骤使用
- 获取标准化股票信息后，将其保存为全局变量或传递给后续步骤使用

**执行步骤**（主 agent 直接操作）：
1. 使用 ptrade search 命令搜索股票，获取标准化的股票名称和代码：
   ```bash
   ptrade search "<用户输入>" --limit 5
   ```
2. 根据搜索结果匹配用户意图，确定正确的股票名称和代码
3. 使用 `ptrade market-summary <股票代码> -f markdown` 获取多周期市场数据：
   - **实时股价**：当前价、昨收、涨跌幅
   - **月K数据**：近6个月走势，含每根K线的开/收/高/低/环比涨跌
   - **周K数据**：近8周走势
   - **日K数据**：近5日走势
   - **分时数据**：当日关键时间点（开盘、早盘高点、午盘低点、午后企稳、收盘）及振幅
   - **股票信息**：
     - 完整的股票名称（中文全称）
     - 正确的股票代码（如：600519.SH、000858.SZ）
2. 如数据获取失败，标注"数据暂不可用，建议使用东方财富/同花顺查看"
3. 使用标准化股票名称(简体中文)和代码继续后续步骤

### 步骤 2: 读取历史分析

创建 subagent，要求 subagent 使用 ptrade CLI 工具读取历史分析报告并生成连续性分析。

**执行原则**（主 agent 操作）：
- 创建专门的 subagent
- 在 subagent 的 prompt 中明确要求其智能选择历史分析数据并保存连续性报告

**任务要求**（传递给 subagent 的 prompt 内容）：
1. subagent 读取 `references/analysis_history_guide.md` 了解执行方法
2. 使用 `ptrade analysis <股票名> --action list --limit 30` 列出最近 30 次分析记录
3. **智能选择报告**：根据时间分布选择短期（最近 1-3 天）、中期（一周左右）和长期（近一个月）的代表性报告
4. 读取选定报告并分析预测准确性、观点演进、建议更新
5. **保存连续性分析报告**到 temp-data 目录（使用 `ptrade temp-data --action save --category history-continuity` 命令）

### 步骤 3: 自适应深度搜索

创建 subagent，要求 subagent 执行深度搜索任务收集新闻和热点信息。

**执行原则**（主 agent 操作）：
- 创建专门的搜索 subagent
- 在 subagent 的 prompt 中明确要求其执行深度搜索

**任务要求**（传递给 subagent 的 prompt 内容）：
1. subagent 读取 `references/deep_search_guide.md` 了解详细执行方法
2. 优先使用 searxng-search-skill（本地部署，免费），遇到验证码切换到 brave-search
3. 获取市场新闻：使用 `ptrade fetch-news --source all --limit 30`
4. 按以下维度执行深度搜索：
   - 公司基本面（财务数据、经营指标、业务进展）
   - 行业生态（竞争格局、行业趋势、产业链）
   - 政策环境（行业政策、宏观影响、地区政策）
   - 实时动态（最近 1-2 周的新闻热点）
5. **保存搜索报告**到 temp-data 目录（使用 `ptrade temp-data --action save --category deep-search` 命令）

### 步骤 4: 获取广发证券专业数据

创建 subagent，要求 subagent 调用 gf-finance skill 执行专业数据获取任务。

**执行原则**（主 agent 操作）：
- 创建专门的 subagent
- 在 subagent 的 prompt 中明确要求其调用 gf-finance skill 完成数据获取

**任务要求**（传递给 subagent 的 prompt 内容）：
1. subagent 读取 `references/gf_finance_guide.md` 了解执行指南
2. 调用 gf-finance skill 获取以下三维度数据：
   - **龙虎榜数据**：净买入/卖出、知名席位动向
   - **财务基本面**：PE/PB估值分位、ROE、现金流
   - **ETF 榜单**：相关ETF涨幅、资金流入
3. **保存广发数据汇总**到 temp-data 目录（使用 `ptrade temp-data --action save --category gf-summary` 命令）

### 步骤 5: 获取模拟盘持仓

**主 agent 操作**：通过调用 paper-trading skill 获取当前模拟盘持仓状态和收益情况。

**执行步骤**：
1. 使用 Skill 工具调用 `paper-trading`，读取该 skill 的使用文档，了解 ptrade 命令的使用方法

2. 尝试调用 `ptrade info <股票名>` 命令获取当前账户完整信息：
   - 如果命令成功，解析返回的资金池、持仓、收益信息
   - 如果返回"未找到账户"或类似错误，则执行步骤 3

3. 如果账户未初始化，自动初始化资金池（默认 500,000 元）：
   ```bash
   ptrade init "<股票名>" --capital 500000
   ```

4. 重新调用 `ptrade info <股票名>` 确认初始化成功并获取账户信息

5. 调用 `ptrade operations <股票名> --days 7` 查看近7日交易记录，核对近期是否已执行过减仓/加仓操作，防止报告给出重复建议

6. 解析返回结果，提取以下关键信息：
   - **资金池状态**：
     - 总资金
     - 可用资金
     - 占用资金
     - 资金使用率
   - **当前持仓**：
     - 持仓数量
     - 持仓成本
     - 当前价格（如有）
   - **收益情况**：
     - 实现盈亏
     - 浮动盈亏
     - 总盈亏
     - 总收益率
   - **近期操作**：
     - 近 7 日买卖记录
     - 最近操作时间与方向

7. 保存持仓和收益状态到变量，供报告生成使用

8. 如果初始化了资金池，在报告中标注"本次分析已创建模拟盘资金池"

**数据整合要点**：
- ✅ 持仓情况将影响投资建议的表述（如空仓可积极建仓，重仓则谨慎）
- ✅ 收益情况用于评估历史操作的有效性
- ✅ 记录当前持仓状态作为报告的"基准状态"
- ✅ 近期操作记录用于校验一致性，避免重复判断加建仓
- ✅ 为后续异步执行交易行为提供基础数据

---

### 步骤 6: 整合中间数据

**主 agent 操作**：使用 ptrade temp-data 命令读取所有 subagent 保存的中间数据

```bash
# 读取历史连续性分析
ptrade temp-data "<股票名>" --action read --category history-continuity

# 读取深度搜索报告
ptrade temp-data "<股票名>" --action read --category deep-search

# 读取广发证券数据
ptrade temp-data "<股票名>" --action read --category gf-summary
```

**数据整合要点**：
- ✅ 确保所有中间数据都已生成并可以读取
- ✅ 提取各数据的关键信息
- ✅ 识别数据之间的关联和矛盾
- ✅ 准备好结构化数据供报告生成使用

### 步骤 7: 生成并保存分析报告

按照三层分析框架组织报告内容，**使用 ptrade CLI 工具保存**至规范化位置。

**三层分析框架**：
1. **公司业务** - 核心业务状态、产品矩阵进展、财务表现
2. **行业趋势** - 行业周期位置、竞争格局变化、政策环境影响
3. **短期线索** - 正面信号、负面信号、催化剂事件

**保存命令**：
```bash
# 步骤 1: 先保存到临时文件
cat > /tmp/{股票名}_analysis_${CLAUDE_SESSION_ID}.md << 'EOF'
<按三层分析框架组织的完整分析报告内容>
EOF

# 步骤 2: 使用 --file 参数归档
ptrade analysis "<股票名>" --action save --file /tmp/{股票名}_analysis_${CLAUDE_SESSION_ID}.md
```

**保存位置**: `stocks_analysis/<股票名>/<股票名>-YYYY-MM-DD-HHMM.md`
- 该命令会自动创建带时间戳的文件名
- 自动创建 Soft链接 `最新分析.md` 指向最新分析

**报告格式要求**：
```markdown
# 📊 <股票名> (<代码>) 每日分析

**分析时间**: ...
**关注焦点**: ...
<!-- 注意：必须使用真实的系统时间，可通过 `date` 命令获取，不要使用项目配置中的 currentDate -->

## 📈 实时股价
| 指标 | 数值 |
|------|------|
| 当前价 | ¥XX.XX |
| 涨跌幅 | 📈/📉 X.XX% |
| 周K趋势 | 近8周涨跌 X% |

## 💰 模拟盘状态
| 资金池状态 | - | 持仓信息 | - | 收益情况 | - |
|------|------|------|------|------|------|
| 总资金 | ¥XX.XX | 当前持仓 | XX股 | 已投入总成本 | ¥XX.XX |
| 可用资金 | ¥XX.XX | 平均成本 | ¥XX.XX | 已实现盈亏 | ¥+XXX.XX / -XXX.XX |
| 已用资金 | ¥XX.XX | 持仓市值 | ¥XX.XX | 浮动盈亏 | ¥+XXX.XX / -XXX.XX |
| 使用率 | XX% | | | 总收益率 | +XX.XX% / -XX.XX |

> 注：如显示"模拟盘未初始化"，表示该股票尚未在模拟盘系统中创建资金池。
> 注：如果没有有持仓/收益数据，对应项目填0即可

## 📋 交易纪律与触发条件

> **⚠️ 条件表格生成规则（强制）**：
> 1. **所有条件信息必须通过 CLI 打印**，agent 不得手写条件价格
>    ```bash
>    ptrade conditions "<股票名>" --format markdown --template all
>    ```
>    - 输出包含：本期触发条件表、硬条件修改审计、上期执行检查
>    - ❌ 禁止: agent 自己估算并写入"移动止损 ¥XX.XX"
> 2. **如需修改条件，必须先通过 CLI 修改**，修改后重新打印
>    ```bash
>    ptrade conditions "<股票名>" --action update --type trailing_stop --price XX.XX
>    ```
> 3. 如果 CLI 输出包含警告（⚠️ LEVEL_3），必须在报告中如实声明
> 4. 条件表格下必须包含: `> **条件来源**: 本期条件来自 ptrade conditions 系统记录`

> **规则摘要**：🔒硬条件=持仓周期内有效，只升不降；🔧软条件=设定日起7天失效，过期需说明调整理由

<!-- 下方直接粘贴 CLI --template all 的完整输出 -->

```bash
ptrade conditions "<股票名>" --format markdown --template all
```

## 📜 历史分析回顾
### 近期分析对比表
| 日期 | 预测方向 | 目标价位 | 建议操作 | 关键观点 | 实际表现 |
|------|----------|----------|----------|----------|----------|
| ... | ... | ... | ... | ... | ... |

### 观点演进分析
- **预测准确性**: ...
- **观点变化**: ...
- **建议演进**: ...
- **风险追踪**: ...
- **关注焦点**: ...

### 利润保护追踪

| 指标 | 数值 |
|------|------|
| 最高浮盈日期 | YYYY-MM-DD |
| 最高浮盈幅度 | +XX.X% |
| 当前浮盈幅度 | +XX.X% |
| 利润回撤幅度 | -XX.X% |
| 当前止损位 | ¥XX.XX |
| 距离止损位 | +X.X% |

> **回撤预警规则**：利润回撤超过 **30%** 时，即使未触及原始止损位，也建议减仓 20% 锁定利润。
> **移动止损规则**：浮盈>20%时，移动止损位 = max(成本价 x 1.05, 原始止损位)；浮盈>50%时，移动止损位 = max(成本价 x 1.15, 原始止损位)；移动止损位一旦上移不得下调。

## 🔍 最新新闻摘要
- 新闻 1
- 新闻 2
- ...

## 📋 三层分析

### 1️⃣ 公司业务
- ...
### 2️⃣ 行业趋势
- ...
### 3️⃣ 短期线索
- ...

## 🏦 广发证券专业数据
### 龙虎榜分析
...
### 财务基本面
...
### ETF 联动
...

## 🎯 涨跌预测
**短期（1-3 个月）**: ⬆️/⬇️/➡️ + 具体价格 + 理由
**中期（3-6 个月）**: ...
**长期（6-12 个月）**: ...

## 💼 投资建议

### 第一步：上期条件触发检查

直接引用上方 CLI `--template all` 输出的「上期触发条件执行检查」结论：
- 是否有上期条件已触发但未执行？ → 如有，本期建议必须包含"立即执行"
- 是否有条件已过期？ → 如有，需进入复审或重新设定
- 利润回撤是否 > 30%？ → 如是，必须建议减仓 20%
- 强制复审触发器是否激活？ → 如是，所有软条件需重设

### 第二步：本期操作建议

| 条件状态 | 本期建议（强制） | 理由 |
|----------|------------------|------|
| 止盈/止损触发 | **执行对应操作** | 纪律优先 |
| 利润回撤 > 30% | **减仓 20%** | 利润保护 |
| 强制复审激活 | **持有 + 重设条件** | 环境已变 |
| 无任何触发 | **持有 / 按趋势操作** | 未达预设条件 |

> ❌ **禁止**：条件已触发仍建议「持有」、利润回撤>30%不减仓、用「基本面未变」替代纪律执行

**当前操作建议**: [买入 / 卖出 / 持有 / 减仓（比例）]

### 第三步：本期新条件设定

如需修改条件，先通过 CLI update，然后重新打印最终条件表：

```bash
# 修改示例
ptrade conditions "<股票名>" --action update --type trailing_stop --price XX.XX

# 打印本期最终条件（含审计表、执行检查）
ptrade conditions "<股票名>" --format markdown --template all
```

> 硬条件（🔒）只升不降；软条件（🔧）今天+7天=失效日期；上期过期未触发的需说明调整理由。

## ⚠️ 风险提示
- 风险 1
- 风险 2
- ...
```

### 步骤 8: 任务完成

输出执行摘要：

```
✅ 股票分析任务完成

📊 股票：<股票名> (<代码>)
🎯 预测：短期⬆️/⬇️/➡️ | 中期⬆️/⬇️/➡️ | 长期⬆️/⬇️/➡️
💼 建议：[买入/卖出/持有/减仓（比例）]

🔔 条件状态：
  - 止损 ¥X.XX (持仓周期内): ⬜ 未触发 / ✅ 已触发
  - 止盈1 ¥Y.YY (至YYYY-MM-DD): ⬜ 未触发 / ✅ 已触发 / ⚠️ 已过期
  - 利润回撤: Z% [正常/⚠️ 超30%]

⚠️ 复审状态：[无/🚨 强制复审已激活]
💡 理由：[核心观点摘要]

💰 模拟盘：已持XX股 / 未初始化 / 空仓
📁 报告位置：stocks_analysis/<股票名>/xxx.md

总耗时：X 分钟
```

## 工具使用说明

### 主要工具概览

| 工具 | 用途 | 参考文档 |
|------|------|----------|
| searxng-search-skill | 深度搜索 | [deep_search_guide.md](references/deep_search_guide.md) |
| gf-finance skill | 广发证券专业数据 | [gf_finance_guide.md](references/gf_finance_guide.md) |
| paper-trading skill | 模拟盘交易系统（股价、交易、分析报告管理） | [../paper-trading/SKILL.md](../paper-trading/SKILL.md) |

## 注意事项

### 执行流程相关
- **搜索迭代**：根据搜索结果质量调整搜索词，进行深度挖掘
- **数据验证**：如股价数据获取失败，在报告中标注"数据不可用"
- **报告保存**：股票分析完成后立即保存，避免丢失

### 工具使用相关
- **Skill 调用**：股价数据、广发数据、模拟盘交易系统通过创建 subagent 调用对应 SKILL 完成，主 agent 无需感知具体路径
  - paper-trading skill：使用 Skill 工具调用 `paper-trading` 自行读取文档了解 ptrade 命令使用方法
- **数据验证**：如数据获取失败，在报告中标注"数据暂不可用"

### 数据分析相关
- **三层分析框架**：必须包含公司业务、行业趋势、短期线索三个层面
- **明确预测**：涨跌预测不可模棱两可，必须给出明确判断（⬆️/⬇️/➡️）
- **风险提示**：至少列出 3 项主要风险
- **新闻数量**：最新新闻摘要至少 5 条

### Subagent 管理
- **必须等待**：所有 subagent 必须等待任务完成并获取返回结果后再继续
- **中间数据保存**：每个 subagent 完成任务后，必须将结果保存到 ptrade 的 `temp-data/<股票名>/` 目录（通过 `ptrade temp-data` 命令）
- **主 agent 读取**：主 agent 使用 `ptrade temp-data --action read --category <类别>` 命令读取临时数据，替代文件路径读取，保持工具使用统一性
- **主 agent 不读取参考文档**：`references/` 目录下的所有 `.md` 文件（如 `analysis_history_guide.md`、`deep_search_guide.md`、`gf_finance_guide.md`）是给 subagent 执行时阅读的。主 agent 无需也不应自己去读这些文件，只需在创建 subagent 的 prompt 中指明让 subagent 读取对应文档即可

### 交易纪律约束（防止预测点漂移）

1. **触发条件必须标注失效日期**：所有软条件必须写清楚失效日期（YYYY-MM-DD），方便后续报告直接判断
2. **过期条件必须列出**：上期已过期但未触发的条件，必须在报告中列出并说明是否进入复审
3. **硬条件只升不降**：止损位一旦上移不得下调；移动止损随利润增加只升不降
4. **操作建议非持有化**：条件触发后必须给出对应动作（减仓/加仓/清仓），不得以"基本面未变"为由维持"持有"
5. **利润保护强制**：利润回撤>30%时必须建议减仓，不得无视
6. **原始目标不可篡改**：历史回顾中的目标价位必须是原始设定值，不得事后修改
7. **复审声明强制**：若本期修改了上期条件，必须在报告中声明修改理由和新旧对比
8. **强制决策优先**：生成报告时，先执行「第一步：上期条件状态检查」，根据检查结果决定操作建议，不得跳过条件检查直接给出"持有"

## 参考

- [deep_search_guide.md](references/deep_search_guide.md) - 自适应深度搜索详细执行方法
- [gf_finance_guide.md](references/gf_finance_guide.md) - 广发证券数据整合指南
- [analysis_history_guide.md](references/analysis_history_guide.md) - 历史分析数据读取指南
- [../paper-trading/SKILL.md](../paper-trading/SKILL.md) - 模拟盘交易系统使用指南（包含股价查询、分析报告管理、临时数据存储）